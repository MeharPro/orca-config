# New Printer (Add Your Files Here)

Drop your printer-related preset exports/files for a new printer here.

If you want those presets baked into the portable zip automatically, copy the relevant files into:
- `configs/portable-overlay/root/` (matching upstream zip paths, e.g. `resources/profiles/...`)

